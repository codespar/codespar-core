{
  "name": "@codespar/example-pix-nfse-skeleton",
  "version": "0.0.0",
  "private": true,
  "description": "P3 walking skeleton — 4-step Pix + NFS-e loop against the OSS MCP bridge with --demo fixtures.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "skeleton": "vitest run --reporter=verbose",
    "validate": "./scripts/validate.sh"
  },
  "dependencies": {
    "@codespar/sdk": "file:../../packages/core"
  },
  "devDependencies": {
    "@codespar/mcp-asaas": "0.1.3",
    "@codespar/mcp-nuvem-fiscal": "0.2.1",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
