{
  "name": "service-invoice-meta-tool",
  "version": "0.0.0",
  "private": true,
  "description": "Service invoice from a natural-language message \u2014 driven by session.send() + aimock against the OSS runtime with mocks-API tool fixtures.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "skeleton": "vitest run --reporter=verbose",
    "validate": "./scripts/validate.sh"
  },
  "devDependencies": {
    "@codespar/mcp-nuvem-fiscal": "0.3.0",
    "@codespar/mcp-z-api": "0.2.1",
    "@copilotkit/aimock": "^1.24.1",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.0",
    "@codespar/types": "^0.10.10"
  }
}
