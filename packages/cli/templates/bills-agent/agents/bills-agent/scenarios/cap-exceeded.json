{
  "name": "cap-exceeded",
  "description": "A payment above the per-payment cap of the mandate: refused by the core, readable, in both modes.",
  "modes": ["human", "mandate"],
  "transcript": "./cap-exceeded.transcript.jsonl",
  "turns": [
    { "input": "paga 3.000 reais pra escola hoje", "decision": "approve" }
  ],
  "expect": {
    "both": { "states": ["denied"], "reasons": ["per_tx_cap_exceeded"], "receipts": 0 }
  }
}
